# Markdown 内容分类拆分工具

一个智能的 Markdown 自动拆分工具，能够识别标题中的分类标签（如 `## 【分类】标题`），并将内容自动拆分为多个独立的 Markdown 文件，支持一键打包下载。

## 核心功能

- 🚀 **智能识别**：自动提取标题中的分类信息。
- 📂 **批量处理**：支持多个文件同时上传处理。
- ⚡ **实时预览**：拆分结果实时展示，支持单个或全部下载。
- 🎨 **深色模式**：现代化的深色 UI 设计，极致的视觉体验。
- 🔒 **隐私安全**：纯前端处理，所有操作均在浏览器本地完成，不上传任何数据到服务器。

## 部署到 Cloudflare Pages

本项目是一个纯静态页面，非常适合部署在 Cloudflare Pages 上。

### 方法 1：通过 GitHub 自动部署（推荐）

1.  将代码推送到你的 GitHub 仓库。
2.  登录 [Cloudflare 控制台](https://dash.cloudflare.com/)。
3.  进入 **Workers & Pages** > **Create application** > **Pages** > **Connect to Git**。
4.  选择你的仓库，点击 **Begin setup**。
5.  在 **Build settings** 中：
    *   **Framework preset**: 选择 `None`。
    *   **Build command**: 留空。
    *   **Build output directory**: 留空（或填写 `/`）。
6.  点击 **Save and Deploy**。

### 方法 2：使用 Wrangler CLI 部署

如果你想从本地直接部署，可以使用 Wrangler：

```bash
# 安装 wrangler
npm install -g wrangler

# 登录
wrangler login

# 部署
wrangler pages deploy . --project-name=markdown-splitter
```

## 技术栈

- HTML5 / JavaScript (ES6+)
- [Tailwind CSS](https://tailwindcss.com/) - 样式框架
- [JSZip](https://stuk.github.io/jszip/) - 客户端 ZIP 打包
- [Cloudflare Pages](https://pages.cloudflare.com/) - 托管平台

